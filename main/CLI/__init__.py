"""
CLI - Command Line Interface for Agent Learn Services

This module provides a CLI interface to interact with the Service layer,
supporting both single-command mode and interactive REPL mode.

Usage:
    # Single command mode
    python -m CLI agent list
    python -m CLI workspace create dev --path /workspace

    # Interactive REPL mode
    python -m CLI repl
"""

__version__ = "0.1.0"
