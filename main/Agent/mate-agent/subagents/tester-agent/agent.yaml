identity:
  name: tester-agent
  description: Agent 测试器 - 负责测试和验证 Agent 功能
  tags:
    - tester
    - validator
    - subagent

category: meta
cost: cheap

model:
  provider: anthropic
  model: claude-haiku-4-5-20251001
  temperature: 0.3
  max_tokens: 4096

limits:
  max_iterations: 5
  timeout: 120

capabilities:
  tools:
    - validate_agent
    - spawn_subagent
    - list_agents

metadata:
  version: "1.0.0"
  parent: mate-agent
  role: subagent
