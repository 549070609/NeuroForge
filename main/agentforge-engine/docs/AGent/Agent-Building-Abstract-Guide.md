# Agent Building Abstract Guide

This guide has been updated to the provider-free architecture.

Use:
- `LLMClient`
- model-level configuration
- `api_type`, `base_url`, `model_name`, `api_key`, `headers`, `timeout`
